import React from "react";

const Base64Image = ({ base64Data, altText, className }) => {
  return (
    <img
      src={`data:image/png;base64,${base64Data}`}
      alt={altText}
      className={className}
    />
  );
};

export default Base64Image;
